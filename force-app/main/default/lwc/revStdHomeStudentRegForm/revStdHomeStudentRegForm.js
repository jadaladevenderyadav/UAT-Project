import { LightningElement } from 'lwc';
import basePath from '@salesforce/community/basePath';

export default class RevStdHomeStudentRegForm extends LightningElement {
    registrationFormPageUrl = `${basePath}/registration-form`; 
}