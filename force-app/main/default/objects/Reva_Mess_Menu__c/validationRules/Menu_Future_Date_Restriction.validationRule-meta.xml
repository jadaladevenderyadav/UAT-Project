<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Menu_Future_Date_Restriction</fullName>
    <active>false</active>
    <errorConditionFormula>AND(
    NOT(ISBLANK(Date__c)),
    NOT(
        OR(
           Date__c = TODAY(),
           Date__c = TODAY() + 1
        )
    )
)</errorConditionFormula>
    <errorMessage>The date must be either today or tomorrow.</errorMessage>
</ValidationRule>
