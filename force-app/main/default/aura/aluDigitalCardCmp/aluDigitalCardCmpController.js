({
    doInit : function(component, event, helper) {
        
        // var urlEvent = $A.get("e.force:navigateToURL");
        // urlEvent.setParams({
        //     "url":"https://reva-university--couat1908--c.sandbox.vf.force.com/apex/ALU_DigitalCard"
        // });
        // urlEvent.fire();
    }
})