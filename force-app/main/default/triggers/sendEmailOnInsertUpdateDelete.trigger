trigger sendEmailOnInsertUpdateDelete on Account (after insert, after update, before delete) {
   /* if (Trigger.isInsert) {
        AccountEmailHelper.handleInsert(Trigger.new);
    }

    if (Trigger.isUpdate) {
        AccountEmailHelper.handleUpdate(Trigger.new, Trigger.oldMap);
    }

    if (Trigger.isDelete) {
        AccountEmailHelper.handleDelete(Trigger.old);
    }*/

}